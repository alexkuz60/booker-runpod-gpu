# requirements.txt — Booker GPU backend on RunPod Serverless (v7, CUDA 12.4)
#
# Изменения v6 → v7:
#   • torch index: cu128 → cu124 (соответствует базовому образу Dockerfile_v7)
#   • Версии torch/torchaudio оставлены прежние (2.6.0) — cu124 wheels существуют.

# RunPod Serverless SDK
runpod>=1.9.0

# Torch stack (CUDA 12.4 wheels)
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchaudio==2.6.0

# Transformers с HiggsAudioV2TokenizerModel
transformers==5.8.1

# OmniVoice через прямой импорт (вариант Б)
# Замени на актуальный pip-pin когда определимся с веткой/коммитом
# omnivoice @ git+https://github.com/k2-fsa/OmniVoice.git@<commit>

# HTTP + audio
httpx>=0.27.0
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.13.0
