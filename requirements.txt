# requirements.txt v5 — Booker GPU backend on RunPod Serverless
# ============================================================================
# Базовый образ: runpod/base:0.6.2-cuda12.8.1 (CUDA 12.8, cuDNN 9, Python 3.11.x).
# Поддержка Blackwell (RTX 5090, RTX 6000 Pro, sm_120) требует CUDA >= 12.8
# и torch >= 2.6 с cu128 wheels.
#
# OmniVoice + omnivoice-server ставятся из git в Dockerfile (не здесь),
# чтобы requirements.txt быстро ре-резолвился при rebuild.
# ============================================================================

# --- RunPod Serverless SDK (жёсткий пин, не нижняя граница) ---
runpod==1.9.0

# --- Torch stack под CUDA 12.8 (Blackwell-ready) ---
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.6.0
torchaudio==2.6.0

# --- HTTP + audio + численная база ---
httpx>=0.27.0
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.13.0

# --- Transformers с HiggsAudioV2TokenizerModel (нужен OmniVoice) ---
transformers==5.8.1

# httpx пиним отдельно — наш subprocess-клиент должен совпадать
# с тем httpx, который тянет fastapi/uvicorn внутри omnivoice-server.
