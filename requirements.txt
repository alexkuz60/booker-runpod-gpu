# Booker GPU backend — v1 diagnostic (RunPod Serverless, CUDA 12.4)
# Базовый образ runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04
# уже содержит torch 2.4.0 + torchaudio cu124, поэтому НЕ переустанавливаем.

runpod>=1.9.0

# OmniVoice — официальный PyPI релиз (k2-fsa/OmniVoice 0.1.5)
omnivoice>=0.1.5

# Утилиты для handler
soundfile>=0.12.1
numpy>=1.26.0
