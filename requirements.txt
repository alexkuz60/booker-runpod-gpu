# requirements.txt — Booker GPU backend on RunPod Serverless
# ============================================================================
# Pinned to match local install (scripts/install-omnivoice.sh) and
# Modal backend (modal_app.py). CUDA 12.1 base image provides cu121 wheels.
# OmniVoice + omnivoice-server are installed from git in the Dockerfile,
# not here, to keep this file fast to re-resolve on rebuild.
# ============================================================================

# RunPod Serverless SDK
runpod>=1.7.0

# Torch stack (CUDA 12.1 wheels resolved by torch index)
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.0
torchaudio==2.4.0

# HTTP + audio
httpx>=0.27.0
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.13.0

# OmniVoice server pulls fastapi/uvicorn itself, but pin httpx so our
# subprocess client matches.
